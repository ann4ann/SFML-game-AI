[project]
name = "git-ops-mcp"
version = "0.1.0"
description = "Simple MCP server for git operations"
requires-python = ">=3.10"
dependencies = [
    "mcp[cli]>=0.1.0",
    "asyncio",
    "gitpython>=3.1.52",
]
